name: CI Pipeline
on:
  push:
    branches: [ FEAT-*, $default-branch ]
  pull_request:
    branches: [ $default-branch ]
jobs:
  ci-workflow:
    runs-on: ubuntu-latest
    uses: calebtraceyco/.github/devops/jobs/go-package.yml@main
